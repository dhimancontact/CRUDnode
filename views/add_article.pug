extends layout
        
block content
    h1 #{pageTitle}
    form(action='/articles/add', method='POST')
        #form-group
            label title: 
            input.form-control(type='text', name='title', value="")
        #form-group
            label body: 
            textarea.form-control(type='text', name='body', value="")
        input.btn.btn-primary(type='submit', value='Submit') 